# Node / React
node_modules/
dist/
.env
.env.local

# .NET
bin/
obj/
*.user
*.suo
.vs/
appsettings.Development.json

# SQLite (optional — remove if you want to commit the seed DB)
# *.sqlite

# OS
.DS_Store
Thumbs.db

# Stray root-level files (should only exist inside BookstoreApp/BookstoreAPI/)
package-lock.json
